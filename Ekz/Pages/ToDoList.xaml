<Page x:Class="Ekz.Pages.ToDoList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Ekz.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800" Loaded="Page_Loaded"
      Title="ToDoList">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="11*"/>
            <RowDefinition Height="64*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Button x:Name="btnAdd" Content="ДОБАВИТЬ" Click="btnAdd_Click"/>
            <Button x:Name="btnEdit" Content="РЕДАКТИРОВАТЬ" Click="btnEdit_Click"/>
        </StackPanel>
        <ListView x:Name="lstData" Grid.Row="1" SelectionChanged="lstData_SelectionChanged" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="2" CornerRadius="10" BorderBrush="Black" Width="800">

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition  Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Vertical" Grid.Column="0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding TypeProd. Name }"/>
                                    <TextBlock Text="|"/>
                                    <TextBlock Text="{Binding Name }"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Артикул"></TextBlock>
                                    <TextBlock Text="{Binding Art  }"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Минимальная  для партнера"></TextBlock>
                                    <TextBlock Text="{Binding MinCostPart }"/>
                                    </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Ширина"></TextBlock>
                                    <TextBlock Text="{Binding Weigh }"/>
                                    </StackPanel>
                            </StackPanel>
                            <TextBlock Text="{}" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
